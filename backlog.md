# Backlog — Post-v1

Rough ideas captured during v1 build. Do NOT spec or build until v1 is
shipped and Dad has used it on ≥3 real listings.

## Likely v2
- **Saved user preferences.** Let the agent set their own defaults: phrases to always include, phrases to always ban, preferred CTA phrasing, default contact/sign-off line, maybe a fourth custom tone. Stored locally for v2. Came from Dad's feedback after v1 — he was editing the same things out of every variant.
- Feedback on generations (thumbs up/down per variant, stored locally)
- Iteration / refinement ("make variant 2 shorter", "more emphasis on the backyard")
- Regenerate a single variant without re-running all 3
- Photo upload → vision model extracts features automatically
- Save favorite variants to a personal library
- Tone custom beyond the 3 presets (slider, or freeform descriptor)

## Maybe
- Multi-agent mode (one agent writes, one agent critiques, final polish pass)
- MLS field auto-fill from an address lookup
- Export to PDF / email directly from the app
- Per-variant word count target
- "Dad's voice" — fine-tune the prompt on his past listings

## Probably not
- Accounts / multi-user
- Payment / subscription
- Real database